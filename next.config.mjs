/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pinimg.com','images.unsplash.com','localhost'], // Add any other domains you need here
    },
  };
  
  export default nextConfig;
  